- 2026-03-25：基于 `origin/main` 创建并推送分支 `automation/testing-harness-20260325`，完成 diff-aware harness 测试自动化增强（`nextest` 配置、`just` 新入口、`scripts/harness_changed.py` 与对应单测）。本地验证通过：`harness-quick`、新增 9 个 Python 单测、Ruff 检查。PR `#306` 已 squash merge 到 `main`。后续观察到该 PR 的 CI 存在与本次核心改动不直接相关的失败：Linux `Clippy Lints` 因 `crates/auroraview-cli/src/packed/utils.rs` 中 Windows 扩展辅助函数在非 Windows 平台触发 `dead_code`，另有 `SDK CI / E2E Tests` 失败，需后续单独跟进。
- 2026-03-25：代码质量审计 + 清理。基于 `origin/main` 创建分支 `refactor/cleanup-unused-deps-and-features`。全面扫描发现并移除了多个 crate 的未使用依赖：`auroraview-bookmarks`（parking_lot, tracing, dirs）、`auroraview-extensions`（dashmap, anyhow, glob）、`auroraview-telemetry`（anyhow, parking_lot）、`auroraview-cli`（rust-embed）、`auroraview-assets`（include_dir）。清理了 5 个 crate 中未实现的 feature flags：`auroraview-dcc`（qt）、`auroraview-assets`（prebuilt, embed）、`auroraview-ai-agent`（python）、`auroraview-desktop`（test-helpers）。为 `auroraview-pack` 的 `PackManager` 添加了 `format_targets()` 方法替代直接 stdout 输出的 `print_targets()`。本地 `cargo check` 和 `cargo clippy -D warnings` 全部通过。PR [#307](https://github.com/loonghao/auroraview/pull/307) 已创建。首次 CI 运行中 Linux Clippy Lints 失败（与 #306 相同的已知问题），追加了第二个 commit 为 `packed/utils.rs` 中 Windows-only 的 extension 辅助函数添加 `#[cfg(target_os = "windows")]` 门控。CI 全部通过后已 squash merge 到 `main`（commit `f35a1ea`）。
- 2026-03-28：测试框架增强。基于 `origin/main` 创建分支 `automation/test-framework-enhancements-20260328`。新增 `insta` (v1.42) 快照测试作为 workspace 级 dev-dependency（root、core、pack、signals），支持 JSON + redactions 特性。新增 `pytest-rerunfailures` (>=14.0) 到 Python 测试依赖组，支持 flaky 测试自动重试。增强 nextest 配置：新增 `flaky-detect` profile（3 次指数退避重试）和 `snapshot` profile（用于 insta 工作流）。新增 7 个 just 命令：`snapshot-review`、`snapshot-accept`、`snapshot-reject`、`snapshot-ci`、`test-flaky-detect`、`test-python-flaky-detect`、`ci-test-python-rerun`。PR [#318](https://github.com/loonghao/auroraview/pull/318) 已 squash merge 到 `main`。
- 2026-03-28：测试自动化增强（第二轮）。基于 `origin/main` 创建分支 `automation/test-automation-enhancements-20260328`。新增 `pytest-json-report` (>=1.5) 和 `pytest-github-actions-annotate-failures` (>=0.2) 到 Python 测试依赖组，提供结构化 JSON 测试输出和 GH Actions 内联注解。新增 nextest `agent` profile（JUnit + 成功/失败输出存储、2 次 fixed 重试、120s 慢超时），专为自动化 agent 循环设计。新增 `scripts/harness_summary.py` 统一收集 nextest JUnit XML + pytest JSON 报告为单一 JSON 摘要。新增 7 个 just 命令：`harness-summary`、`harness-summary-file`、`test-python-json`、`test-rust-agent`、`harness-agent`、`ci-test-python-json`。新增 `.github/workflows/auto-merge.yml` 自动为 `automation/*` 分支 PR 开启 squash auto-merge。新增 11 个单元测试（全部通过）。PR [#320](https://github.com/loonghao/auroraview/pull/320) 已 squash merge 到 `main`。
- 2026-03-29：变异测试与覆盖率集成。基于 `origin/main` 创建分支 `automation/testing-coverage-mutations-20260329`。新增 `.github/workflows/mutation-testing.yml`：PR 增量变异测试（仅对变更的 Rust 文件运行 cargo-mutants，结果自动评论到 PR）、每周日全量变异扫描（5 个核心 crate 并行）、手动 dispatch 单 crate 测试+分片支持。新增 `scripts/harness_coverage.py`：统一收集 Rust 覆盖率（cargo-llvm-cov JSON）、Python 覆盖率（coverage.xml）、变异分数（mutants.out/）为单一 JSON 报告。新增 nextest `mutation` profile（0 重试、45s 超时、fail-fast）。增强 `.cargo/mutants.toml` 添加 `additional_cargo_test_args` 自动传递 nextest profile。新增 3 个 just 命令：`mutants-diff`、`harness-coverage`、`harness-coverage-file`。新增 14 个单元测试（全部通过）。PR [#321](https://github.com/loonghao/auroraview/pull/321) 已创建，auto-merge 已开启，等待 CI 通过后自动合并。
