[package]
name = "{{ name }}"
version = "0.1.0"
edition = "2021"

# Generated by AuroraView Pack with PyOxidizer integration

# Standalone project - not part of any workspace
[workspace]

[dependencies]
wry = "0.53"
tao = "0.34"
dirs = "6.0"
{% if embed_assets -%}
rust-embed = { version = "8.5", features = ["compression"] }
mime_guess = "2.0"
{%- endif %}

# PyEmbed for embedded Python runtime
# Using AuroraView's maintained fork with modern PyO3 support
pyembed = { git = "https://github.com/loonghao/PyOxidizer", branch = "auroraview-maintained" }

[build-dependencies]
pyo3-build-config = { version = "0.22", features = ["resolve-config"] }

[[bin]]
name = "{{ name }}"
path = "src/main.rs"

[profile.release]
opt-level = "z"     # Optimize for size
lto = true          # Enable Link Time Optimization
codegen-units = 1   # Reduce parallel codegen for smaller binary
panic = "abort"     # Don't include unwinding code
strip = true        # Strip symbols



