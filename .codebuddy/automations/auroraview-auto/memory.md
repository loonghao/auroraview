- 2026-03-25：基于 `origin/main` 创建并推送分支 `automation/testing-harness-20260325`，完成 diff-aware harness 测试自动化增强（`nextest` 配置、`just` 新入口、`scripts/harness_changed.py` 与对应单测）。本地验证通过：`harness-quick`、新增 9 个 Python 单测、Ruff 检查。PR `#306` 已 squash merge 到 `main`。后续观察到该 PR 的 CI 存在与本次核心改动不直接相关的失败：Linux `Clippy Lints` 因 `crates/auroraview-cli/src/packed/utils.rs` 中 Windows 扩展辅助函数在非 Windows 平台触发 `dead_code`，另有 `SDK CI / E2E Tests` 失败，需后续单独跟进。
- 2026-03-25：代码质量审计 + 清理。基于 `origin/main` 创建分支 `refactor/cleanup-unused-deps-and-features`。全面扫描发现并移除了多个 crate 的未使用依赖：`auroraview-bookmarks`（parking_lot, tracing, dirs）、`auroraview-extensions`（dashmap, anyhow, glob）、`auroraview-telemetry`（anyhow, parking_lot）、`auroraview-cli`（rust-embed）、`auroraview-assets`（include_dir）。清理了 5 个 crate 中未实现的 feature flags：`auroraview-dcc`（qt）、`auroraview-assets`（prebuilt, embed）、`auroraview-ai-agent`（python）、`auroraview-desktop`（test-helpers）。为 `auroraview-pack` 的 `PackManager` 添加了 `format_targets()` 方法替代直接 stdout 输出的 `print_targets()`。本地 `cargo check` 和 `cargo clippy -D warnings` 全部通过。PR [#307](https://github.com/loonghao/auroraview/pull/307) 已创建。首次 CI 运行中 Linux Clippy Lints 失败（与 #306 相同的已知问题），追加了第二个 commit 为 `packed/utils.rs` 中 Windows-only 的 extension 辅助函数添加 `#[cfg(target_os = "windows")]` 门控。CI 全部通过后已 squash merge 到 `main`（commit `f35a1ea`）。
