# Unit tests for the auroraview.integration.qt module.
#
# These tests exercise individual mixin methods against minimal host stubs
# (no live QApplication or WebView2 runtime required), so they run on
# headless CI without display or native dependencies.

