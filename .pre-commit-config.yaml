repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-yaml
      - id: check-toml
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: ["--fix", "--select=E,W,F,I,B,C4", "--ignore=E501,B008,C901"]
        files: "^(python|tests|examples|gallery)/"
      - id: ruff-format
        files: "^(python|tests|examples|gallery)/"
  - repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
    rev: v9.21.0
    hooks:
      - id: commitlint
        stages: [commit-msg]
        additional_dependencies: ['@commitlint/config-conventional']
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: python scripts/run_cargo_hook.py fmt --all --
        language: system
        types: [rust]
        pass_filenames: false
      - id: cargo-clippy
        name: cargo clippy
        entry: python scripts/run_cargo_hook.py clippy --workspace --all-targets -- -D warnings
        language: system
        types: [rust]
        pass_filenames: false
