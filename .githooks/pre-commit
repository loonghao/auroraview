#!/usr/bin/env sh
set -eu
if ! command -v vx >/dev/null 2>&1; then
  echo "[pre-commit] vx not found; install from https://github.com/loonghao/vx" >&2
  exit 1
fi
echo "[pre-commit] Running prek with .pre-commit-config.yaml"
exec vx prek run --stage pre-commit --show-diff-on-failure
